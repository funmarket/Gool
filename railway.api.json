{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run db:generate && npm -w @hooma/database run build && npm -w @hooma/contracts run build && npm -w @hooma/api run build"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "NODE_ENV=production npm run db:migrate:deploy && NODE_ENV=production npm -w @hooma/api run start",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
