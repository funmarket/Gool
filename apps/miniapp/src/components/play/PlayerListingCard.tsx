import { PinIcon } from '../../icons/PinIcon';
import { PhoneIcon } from '../../icons/PhoneIcon';
import './PlayerListingCard.css';
export type PlayerListingCardProps={displayName:string;photoUrl?:string|null;age?:number|null;city?:string|null;houma?:string|null;lookingFor:'MATCH'|'TEAM'|'BOTH';contactLabel?:string|null;onContact?:()=>void};
export function PlayerListingCard(props:PlayerListingCardProps){return <article className="player-listing-card-pro"><div className="player-listing-photo">{props.photoUrl?<img src={props.photoUrl} alt={props.displayName}/>:<span>{props.displayName.slice(0,1).toUpperCase()}</span>}</div><div className="player-listing-body"><div className="player-listing-name"><strong>{props.displayName}</strong>{props.age!=null&&<span>{props.age}</span>}</div>{props.city||props.houma?<div className="player-listing-location"><PinIcon size={14}/>{[props.city,props.houma].filter(Boolean).join(' · ')}</div>:null}<div className="player-listing-status">Looking for {props.lookingFor.toLowerCase()}</div>{props.contactLabel&&props.onContact?<button type="button" onClick={props.onContact}><PhoneIcon size={15}/>{props.contactLabel}</button>:null}</div></article>}
